# exec_計画

## 対象タスク
- Connect6 専用の行動表現と探索・学習フローを再設計し、二手同時配置でも順序依存が発生しないようにする。
- MCTS・自己対局・学習パイプライン・ニューラルネットの方策ヘッドを、Connect6 の合成アクション空間に対応させる。
- 自己対局ログおよび学習データで、順序の異なる同型手が同一ラベルとして扱われるよう正規化処理を実装する。
- 探索効率低下を防ぐため、Connect6 専用の候補手フィルタリング（近傍制限など）の設定項目とロジックを導入する。
- 新仕様を説明するドキュメントとテストを整備し、既存ルール（tic_tac_toe）との後方互換性を保つ。

## 現状整理
- `game.State` は 1 手ずつ石を置く前提で `legal_actions()` が盤上インデックスの一次元配列を返す。Connect6 ルールでも A→B と B→A が別系列のまま探索されてしまう。
- `mcts.Node` は盤面サイズ二乗長の numpy 配列を前提としており、行動集合が固定長であることを仮定している。
- `network.Net` の方策ヘッドは `board_size^2` 次元の出力のみをサポートしており、複数手をまとめた行動表現に対応していない。
- 自己対局 (`app/training_loop.py`) や `EpisodeStep.policy_target` は単一手インデックスの分布を扱う。順序の異なる同型手は統合されていない。
- Connect6 特有の手番内二手処理に関するユニットテストやドキュメントは存在しない。

## 問題点
- MCTS で A→B と B→A が別ノード化されるため、訪問統計が分散し探索効率が大幅に低下する。
- 自己対局で得られる方策ラベルが順序付きのまま保存され、ポリシーネットが対称性を学習できずバイアスが生じる。
- 行動空間をそのまま組合せに拡張すると `C(N^2, 2)` に膨張し、ネットワーク出力や探索コストが実運用に耐えなくなる。
- 既存コードは固定長方策ベクトルに依存しており、Connect6 の合成アクション導入には広範囲のコード変更が必要。

## 重要度（優先度）
1. **行動表現と MCTS の正規化実装**: 順序依存の解消は探索・学習の土台となるため最優先。
2. **ネットワーク方策ヘッドとデータフロー更新**: 新しい行動空間に適合させないと自己対局・学習が機能しないため高優先度。
3. **候補手フィルタリングと設定項目追加**: 計算量対策として重要。実装の複雑度は高いが、探索実行時の現実的な性能を確保するため中〜高優先度。
4. **テスト・ドキュメント整備**: 上記変更の妥当性を検証し、他開発者が新仕様を理解できるようにするため中優先度。

## 改修方針
1. **合成アクション空間ユーティリティの新設**
   - `connect6/actions.py`（新規）を作成し、以下の API を提供する。
     - `enumerate_single_actions(board_size)` → 単手インデックス一覧。
     - `iter_pair_actions(empty_indices)` → 空点リストから昇順整列したペア `(i, j)`（`i < j`）を生成。
     - `encode_pair(i, j, board_size)` / `decode_pair(action_id, board_size)` でユニークな整数 ID とペアを相互変換。公式: `S = board_size^2`, `pair_index = i * (2*S - i - 1) // 2 + (j - i - 1)`, `action_id = S + pair_index`。
     - 近傍制限ユーティリティ（例: `filter_pair_candidates(pairs, anchors, max_distance)`）。
   - 単手アクションは ID `[0, S)`、二手アクションは `[S, S + C(S, 2))` に割り当てる。

2. **Game State の合法手生成と適用処理を拡張**
   - `State.legal_actions()` をルール別に分岐し、Connect6 では `ActionSpace` ヘルパーを用いて合法な合成アクション ID リストを返す。
   - 初手は単手 ID を返し、それ以外は `encode_pair` で得た ID を返す。候補数が多い場合は、設定に基づくフィルタ（例: 既存石からマンハッタン距離 `<= d`、またはネットワーク事前分布上位 `k` の空点）を適用。
   - `State.play(action)` は ID を受け取れるよう拡張し、単手はそのまま、ペア ID は `decode_pair` で得た 2 座標を昇順リストにして順に `play` する。
   - `record_string` は複合手を `(A1,B2)` のようなフォーマットで記録するか、2 手連続文字列に分解して既存互換を保つ。後続処理と照らして決定する。

3. **MCTS ノードと探索ループの再設計**
   - `Node` クラスを固定長配列前提から脱却させ、`actions: List[int]` を保持した上で numpy 配列を動的確保する（長さ = `len(actions)`）。`state.legal_actions()` の順序をノード内で保存し、`action_index_map` を構築。
   - `Tree.search` では Connect6 の合法手リスト取得後にネットワーク出力を同じ順序でマスクし、Dirichlet ノイズも対応させる。
   - 子ノード遷移時には、合成アクション ID を `ActionSpace` でデコードして `state.play` に渡す。探索木キー (`state.record_string()`) は既存形式で十分か要確認。必要なら Connect6 用にソート済み合成アクション表現を採用。
   - 探索の PV 生成や visit count 参照でも `Node.actions` を利用して正しいアクション ID を復元する。

4. **ニューラルネットの方策ヘッドを柔軟化**
   - `Net` に `policy_size` を算出する補助メソッドを追加し、Connect6 時は `board_size^2 + C(board_size^2, 2)` を返す。
   - 既存の `conv_p2` を `nn.Linear` に置き換え、最終出力次元を `policy_size` に設定。KataGo 版も同様に改修する。
   - 推論時は `state.legal_actions()` で得た ID のみを残すマスクを適用し、`softmax` 後に再正規化する。未使用次元は `-inf` ログ確率でマスク。
   - Connect6 のペア候補数が大きいことを考慮し、`policy_size` が閾値を超える場合の重み初期化やメモリ消費をチェックする。

5. **自己対局・学習データフローの整合性確保**
   - `app/training_loop.py` で `EpisodeStep.policy_target` に格納する分布長を `policy_size` に合わせる。MCTS の訪問回数ベクトルを合法手 ID に沿って埋め込み、未訪問手は 0 とする。
   - 最終的な勝敗ラベルは既存処理を流用可能。Connect6 用の温度スケジュール調整（必要なら設定化）。
   - `training.EpisodeSampler` のスタック形状も新方策次元で動作することを確認。

6. **設定ファイルとドキュメントの更新**
   - `GameConfig` または新しい Connect6 専用設定（例: `connect6_max_pair_distance`, `connect6_topk_second_candidates`）を追加し、`GameConfig` にネストするか `MCTSConfig` に設ける。
   - `docs/README.md` に Connect6 行動空間の設計・ID マッピング・候補手制限の仕組みを追記する。

## 作業ステップ
- [ ] `connect6/actions.py` を新設し、合成アクション ID 変換ユーティリティと候補フィルタリング関数を実装する。
- [ ] `config` に Connect6 用パラメータを追加し、既定値とバリデーションを定義する。
- [ ] `game.State` の合法手生成・`play` 処理を合成アクション ID 対応に改修する。
- [ ] `mcts.Node` / `Tree` を動的行動集合に対応させ、合成アクションでの探索ロジックを実装する。
- [ ] `network.Net`（basic / katago）の方策ヘッド出力次元を `policy_size` に合わせる改修とマスク処理を導入する。
- [ ] 自己対局 (`app/training_loop.py`) と `training.py` の方策ターゲット処理を新行動空間に対応させる。
- [ ] Connect6 用のユニットテストを追加する（例: 合成アクション ID の相互変換、合法手生成、MCTS が同型手を統合できること）。
- [ ] `docs/README.md` などのドキュメントに行動表現と設定パラメータの変更点を追記する。

## ドキュメント更新方針
- `docs/README.md` に「Connect6 の行動空間」「合成アクション ID マッピング」「探索・学習フロー変更点」を章立てで追加。
- `connect6考察.md` で採用した解決策（合成アクションと候補制限）の要約を追記し、実装済みであることを記載する（必要に応じて脚注）。
- 新規設定項目を `config` セクションで説明し、推奨値や調整方法を記述する。

## テストと検証
- Connect6 盤面で合法手が合成アクション ID として正規化されることを確認する単体テスト。
- `encode_pair` / `decode_pair` の往復一致テスト、および候補手フィルタリングの境界ケース検証。
- 改修後の MCTS で A→B と B→A が同一アクションに統合されることを確認する統計テスト（例: 同一局面での合法手数比較）。
- `tests/test_training_pipeline.py` に Connect6 ケースを追加し、方策ターゲットの形状と正規化を検証。
- `pytest` 全体を実行し、既存ルールでの回帰がないことを確認。

## リスクとフォローアップ
- **出力次元の増大**: `C(N^2, 2)` が大きい場合に GPU メモリや学習速度が悪化する可能性。フィルタリング閾値や候補数を設定で調整できるようにする。
- **MCTS の計算量**: 合法手列挙がボトルネックになる恐れ。必要に応じてキャッシュや遅延生成を導入する余地を残す。
- **既存データ互換性**: 旧形式の自己対局ログは新形式と互換性がない。移行戦略（再生成 or 変換スクリプト）の検討が必要。
- **テスト充実度**: 探索木の挙動は複雑なため、実装後にさらなる統計的テストや自己対局評価を追加検討する。

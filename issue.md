# リファクタリング課題一覧

## 対応済み
- [x] グローバル設定 `cfg` への強い結合を排除し、`AppConfig` を明示的に渡す構造へ変更。
- [x] `State` と `Net` の初期化依存を切り離し、盤面サイズを `GameConfig` から受け取るように整理。
- [x] 学習ループを `training.Trainer` と `EpisodeSampler` に分割し、最適化処理を注入可能にリファクタリング。
- [x] MCTS のログ処理を `SearchReport` コールバックへ分離し、探索ロジックとの混在を解消。

## 1. グローバル設定オブジェクトへの強い結合
- `cfg` を各モジュールで直接 import しており、設定値を変更した際にインポート順序に依存した初期化が走ってしまう。
- 例として `game.py` や `network.py` では、クラス初期化時に `cfg` を読み取っているため、テストや再読み込み時に副作用を切り分けにくい。
- 設定をコンストラクタ引数として受け渡すか、依存性注入用のヘルパーを用意して初期化の明示性を高める必要がある。

## 2. `State` と `Net` の初期化が密結合
- `network.Net` のコンストラクタで毎回 `State()` を生成して盤面サイズを取得しており、ゲームロジックと推論ネットワークが直接結び付いている。
- 盤面サイズを設定ファイルから直接受け取れるようにする、あるいは `State` から必要情報のみを引き渡すインタフェースを切り出すことで、ユニットテスト時の依存関係を削減できる。
- 併せて `State.feature()` の戻り値仕様を明文化し、ネットワーク側での前処理と責務を分離したい。

## 3. 学習ループの責務肥大化
- `training.train` 内でネットワークの生成、最適化設定、損失計算、ロギングまでを一括で担当しており、個別にテスト・再利用しづらい。
- `main.self_play_and_train` からも直接 `train` を呼び出しているため、学習ステップの差し替えや評価フローの変更時に大幅な改修が必要になる。
- 最低限、学習データ生成とパラメータ更新処理をモジュール化し、オプティマイザ設定やスケジューラ適用を外部から注入できるようにリファクタリングしたい。

## 4. MCTS の探索ログと探索ロジックの混在
- `mcts.Tree.think` は探索処理の途中でログ出力や経過時間表示を直接行っており、ロジックと UI が密結合になっている。
- ログ出力の有無を `show` フラグで切り替えているものの、標準出力への依存はテスト自動化を難しくする。
- ログを発行するコールバックを引数として受け取る、または専用のロガーを導入するなどして、探索ロジックと表示処理を分離する必要がある。

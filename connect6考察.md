# Connect6におけるMCTSとモデル設計の課題と対応策

## 背景
Connect6では先手の初手のみ1手で、その後は各手番で2手ずつ石を配置する。この「二手同時配置」の規則により、座標Aと座標Bに石を置く手は、順序に関係なく同一の結果となる。しかし、現在のMCTS実装やモデル設計が通常の一手ずつ打つゲームを前提にしていると、次のような問題が発生する。

- 探索ではA→BとB→Aが別の分岐として扱われ、同一局面に重複到達する。
- 自己対局データではA→BとB→Aがともに正しい手であるはずなのに、一方しか正解ラベルとして認識されず、ポリシー学習に歪みが生じる。

以下では、上記課題の解決に向けた設計方針と具体的な対策を整理する。

## 課題1: MCTSで同型手順が大量に生成される
### 問題の詳細
通常のMCTSでは「一手を指すごとに状態が遷移する」ことを前提にノード展開が行われる。Connect6で同じ2点に石を置く手が順序の違いで別ノードとして扱われると、以下の弊害が生じる。

- 同一局面への重複探索により、シミュレーションコストが倍増する。
- 統計が分散し、訪問回数・価値推定が適切に集約されない。
- 収束が遅くなり、MCTSが本来の強さを発揮できない。

### 対応策
1. **アクションの正規化 (ソートによる一意化)**
   - 石を置く2点を座標の小さい順に並べてタプル化し、`(min(A,B), max(A,B))` の形でノード識別子を作成する。
   - ノード展開時に正規化した識別子を使うことで、A→BとB→Aを同一ノードにマージできる。
   - 正規化はゲームロジック層で完結でき、探索アルゴリズムへの影響が小さい。

2. **二手を一つの合成アクションとして扱う**
   - MCTSの展開ルールを変更し、各手番で選択される行動を「2点同時に打つ合成アクション」と定義する。
   - 子ノード生成時に利用可能な空点の組み合わせを生成し、順序なしの組を直接展開する。
   - ノードの訪問回数・価値が重複せず、探索木の分岐数も正確に評価できる。
   - 合成アクションの数は盤面サイズによって膨大になる可能性があるため、「第一石を候補点に限定し、第二石を近傍中心に生成する」「モンテカルロサンプリングで第二石候補を抽出する」などの枝刈りも併用する。

3. **トランスポジションテーブルの導入**
   - 盤面状態をキーにして既存ノードを再利用する仕組みを導入する。
   - 合成アクション導入が困難な場合でも、順序を入れ替えた結果の同一局面を同一ノードとして扱えるようにすることで、統計の重複集約が可能になる。

## 課題2: 学習データで正解ラベルが片方に偏る
### 問題の詳細
AlphaZero形式の学習では、自己対局で得られた各手番のポリシー分布と価値を教師信号とする。Connect6の場合、探索がA→Bを選んだ局面とB→Aを選んだ局面は同一のはずだが、順序を区別したまま記録すると一方の確率のみが高く、対称な手が「誤り」と学習される。これにより以下が発生する。

- ポリシーネットワークが対称性を学習できず、同型の手に大きな確率差をつける。
- 探索時の先行分布に偏りが生じ、MCTSの探索効率も再び低下する。

### 対応策
1. **ポリシー分布の対称化前処理**
   - 自己対局データを保存する際に、行動を正規化した合成アクション表現に変換する。
   - 同じ2点に対応する確率を合算して1つのエントリにまとめ、順序に依存しない教師分布とする。
   - これにより、MCTSから得た探索統計と学習データが一貫した表現になる。

2. **ネットワーク出力の再設計**
   - 出力ヘッドを「単点ポリシー」ではなく「二点組み合わせポリシー」に合わせる。具体的には以下の方針が考えられる。
     - **階層的出力**: 1つ目の石の分布と、選択済みの石を条件とした2つ目の石の分布を別々に出力し、損失計算時に順序非依存の正解値に対して平均化する。
     - **二次元対称行列出力**: 盤面サイズ×盤面サイズの対称行列を出力し、上三角のみを使用して順序の無い組み合わせを表現する。
   - いずれの場合も、損失計算時に正規化したターゲット分布を使い、順序の違いによる誤差を避ける。

3. **データ拡張による補強**
   - 正規化を行った上で、A→BとB→Aの双方をデータとして用意し、同一教師値を割り当てる。
   - 局面対称性 (回転・反転) と合わせてデータを増強し、ネットワークに対称性を強制的に学習させる。

## 追加検討事項
- **探索・学習の一貫性**: MCTSとネットワークが同じアクション表現を共有すると、探索結果と学習データの齟齬が解消され、訓練が安定する。実装変更時は、自己対局ログ、リプレイバッファ、推論APIなど、全てのデータフローが同じ表現に統一されているかを確認する必要がある。
- **計算量対策**: 合成アクション化で分岐数が大幅に増える場合は、候補点の事前フィルタリング (例: 既存石から一定距離以内の点のみを候補とする) や、探索深度に応じたビーム幅制限、ポリシーネットのトップKサンプリングなどの枝刈りを組み合わせると現実的な計算コストに抑えられる。
- **価値関数への影響**: 合成アクションに切り替えると中間状態 (1手だけ置いた状態) が探索木から消えるため、価値推定も終局との距離が短くなる。この影響を吸収するために、価値ネットの訓練データも新しい遷移構造で再取得することが望ましい。

以上の対策を組み合わせることで、Connect6特有の二手同時配置に起因するMCTSと学習の不整合を解消し、探索効率と学習安定性を高めることができる。


# 6目並べにおける連続手の扱いに関する考察

## 背景
6目並べ（Connect6）は、初手のみ1石、それ以降は各手番で2石を置くルールになっている。標準的なアルファゼロ系の実装では、行動が「単一座標への着手」として定義されているため、2石連続配置をそのまま扱うと Monte Carlo Tree Search (MCTS) や方策学習において以下の問題が生じる。

- 2石の配置順序 A→B と B→A が同一局面を生むにもかかわらず、探索木では別ノードとして扱われる。
- 自己対局データから得られる正解ラベル（最終的な勝率や方策）が、順序に依存して一意に定まらず、片方のみを正解と誤認しやすい。

## 問題の詳細
1. **探索効率の低下**: MCTS ではノードが重複することで、同等の局面に対して統計が分散し、訪問回数が薄くなる。これにより価値推定が不安定になり、探索効率が著しく落ちる。
2. **方策学習の不整合**: ネットワークの出力と教師データの整合性が崩れ、A→B か B→A のどちらかにのみ確率質量が集中してしまう。これが収束を遅らせたり、学習済み方策のバイアスとなる。

## 解決に向けた基本方針
### 1. 行動表現の正規化
- **無順序対の扱い**: 2つの座標を昇順（例: 1次元インデックスで小さい順）に並べ替えて 1 つの「行動」として表現する。MCTS の展開時にこの正規化を適用すれば、A→B と B→A は常に同一ノードに合流する。
- **専用の行動インデックス**: ボードサイズが N×N のとき、組み合わせ数は \(\binom{N^2}{2}\) となるため、直接全組を出力するのは現実的でない。そこで、`(first, second)` を辞書順でユニークに符号化し、`first` を選択後に合法手から `second` を選ぶ 2段階表現に落とし込む。ネットワーク出力を「最初の1手を選ぶ確率」と「その手以外から2手目を選ぶ条件付き確率」に分解すると、順序性を保ちながらも正規化された表現を実現できる。

### 2. 探索木でのノード統合
- **トランスポジションテーブルの利用**: 盤面と手番だけでノードを識別し、順序の違いで生じる重複ノードをハッシュで統合する。2石配置後の盤面をキーにすれば、A→B と B→A が同じノードに集約される。
- **展開時の制約**: 既に 1 石目を仮置きしてから 2 石目を探索する実装では、2 石目の展開後に盤面を正規化（並べ替え）してから子ノードへ遷移するように改修する。これにより、木構造の分岐が整理され、訪問統計が共有される。

### 3. データセット側の対処
- **教師ラベルの統合**: 自己対局から生成する方策ターゲットでは、A→B と B→A を同一のバケットに集約し、確率を合算して正規化する。これにより、学習時の教師信号が順序に依存せず、両方の着手が同等に扱われる。
- **対称性データ拡張**: 盤面の回転・反転対称性だけでなく、2石の入れ替えもデータ拡張として組み込む。これにより、モデルが順序に不変な表現を獲得しやすくなる。

### 4. ネットワーク設計の調整
- **二段階ポリシーヘッド**: 方策ヘッドを 2 つ用意し、`policy_first` と `policy_second` に分ける。推論時は `policy_first` で最初の石をサンプリングし、残りの合法手から `policy_second` を用いて 2 石目を選び、最後に順序正規化を行う。学習時は 2 段階の方策に対して交差エントロピー損失を計算し、両者を合算する。
- **マスキング処理**: 2 石目を選ぶ際に、既に占有されている座標と 1 石目に選んだ座標をマスクする。これを MCTS とニューラルネット双方で一貫して実装することで、合法手集合が揃う。

### 5. 実装運用の注意点
- **初手特例の切り出し**: 初手のみ1石である点を MCTS のルートノードで扱い、以降は常に「2石セットの行動」として扱う。初手ルールをコード上で明確に区別することで、特殊ケースが MCTS に混入するのを防ぐ。
- **統計の再計算**: 正規化後は、自己対局履歴の再構築や過去データの再処理が必要になる。既存データを利用する場合は、順序正規化を施してから再学習する。
- **評価ロジック**: 推論結果の整合性を確認するために、1手ずつ配置するモードと2石同時配置モードの両方でプレイアウトが一致するかをユニットテストする。

## まとめ
6目並べのような「同一手番で複数石を置く」ゲームでは、行動を順序付きの2連続着手として扱うと探索・学習の両面で冗長性と不整合が生じる。行動表現の正規化、探索木のノード統合、教師データの統合、そしてネットワーク設計の2段階化を組み合わせることで、A→B と B→A を同一とみなすアルゴリズム基盤を整えられる。これにより、MCTS の探索効率と方策学習の安定性が向上し、Connect6 に特化した AlphaZero 系実装の性能改善が期待できる。


# 6目並べ（Connect6）のMCTS/モデル設計に関する考察

## 背景
6目並べでは初手のみ1手打ち、それ以降は同一ターンに2手連続で石を配置する。このルールにより、盤面遷移は順序のない石の組み合わせとして扱うべきであるにもかかわらず、`mcts.py` の通常実装では「Aに打ってからB」「Bに打ってからA」が別ノードとして展開され、探索と学習の双方で非効率が発生している。

## 課題の整理
- **探索の重複**: MCTSの子ノードが順序付きの二手として分岐するため、実質的に同じ局面が重複して生成される。これによりノード数が指数的に増え、訪問回数や価値の分散を招く。
- **方策ラベルの一意性崩れ**: 教師データを作る際、正解となる着手が順序のない組み合わせで複数存在するが、順序付き出力にマッピングすると一方だけが正解と扱われ、損失計算で矛盾を生む。

## アクション表現の再設計
1. **組み合わせとしての正規化**
   - 二手の座標をソート（例: インデックス小さい順）し、`(min(p, q), max(p, q))` を1つのアクションIDに写像する。MCTSではこのアクションIDでノードを一意化する。
   - 組み合わせ数は `C(n, 2)` で表現でき、辞書順インデックス化（例: 三角数テーブル）を用いて出力ベクトルに落とし込む。

2. **階層的展開**
   - 1手目を通常通り子ノード展開し、そのノードの中で2手目の候補を評価する「二層選択」を行う。2手目選択時に順序を固定（例: 1手目より大きいインデックスのみ探索）することで順序重複を排除する。
   - この方法は二層目をプルーニングしやすく、部分的な評価関数（例: 「仮置き」した石の価値）を組み込める。

3. **トランスポジションテーブル**
   - 子ノード生成後に盤面のZobristハッシュを用いたトランスポジションテーブルで同一局面を統合する。組み合わせ正規化と併用するとほぼ完全に重複を除去できる。

## ニューラルネット方策ヘッドの工夫
- **組合せ出力マッピング**: 方策ヘッドを `n^2` 出力ではなく `C(n^2, 2)` 出力に拡張し、上記ソート済みペアに対応させる。計算量が大きい場合は、
  - 第1手のヒートマップ `p1` と、選んだ第1手に条件付きの第2手分布 `p2` を出力する二段構成にし、`policy(p, q) = p1[p] * p2[p][q]`（`q` の候補は `p` より後の座標のみ）とする方法が現実的。
- **順序入れ替えのデータ拡張**: 既存の順序付き方策出力を維持する場合でも、教師データ生成時に `(A, B)` と `(B, A)` 両方を同一確率で加算し、損失計算時に両者を同一ターゲットとして扱うことで偏りをなくす。ラベル正規化時には合計確率が1になるよう平均化する。

## 学習データ生成時の配慮
- 自己対局で得られる遷移から方策ターゲットを作るとき、最終的な着手を組み合わせ正規化してから集計する。これにより同じ局面の訪問回数が同一バケットに集まり、価値推定の分散が抑えられる。
- MCTS探索途中の`visit_count`を方策に反映する際も、ペアをソートし合算した上で正規化する。

## 実装時の追加検討事項
- **初手特例**: 初手は1手のみのため、方策ヘッドに「単手アクション」を追加するか、ダミーのパス石を組み合わせて `C(n^2+1, 2)` とするなど整合性をとる。
- **メモリ効率**: `C(n^2, 2)` が大きくなるため、ビットボードや座標圧縮でアクションを管理し、重複排除のための辞書構造も効率化する。
- **探索の深さ調整**: 二手一組のため1ターンあたりの分岐が増える。UCTの温度パラメータや探索回数を再チューニングし、価値・方策の信頼度に応じて探索幅を制御する。

## まとめ
- 6目並べの二手連続ルールは、MCTSでも学習でも「順序のない組み合わせ」として扱わなければデータが歪む。
- アクションを組み合わせ正規化し、探索・ラベル生成・ネットワーク出力の整合性を取ることで、探索重複と教師信号の偏りを解消できる。
- 初手特例や出力次元の増加といった副作用に注意しつつ、階層的方策出力やトランスポジションテーブルの活用で実装負担を抑えられる。

# Connect6 における二手連続打ちの扱いに関する考察

## 背景
Connect6 では初手のみ一手、その後は両陣営が一度の手番で二手連続して石を打つ。この仕様により、盤面上の座標 \(A\) と \(B\) に同一手番で石を置く操作は、順序が異なっても結果が同じになる。したがって「\(A\rightarrow B\)」と「\(B\rightarrow A\)」が同一局面を生成するが、標準的な MCTS 実装ではこれらを別の分岐として扱ってしまう。この挙動は次の問題を引き起こす。

- 同じ結果になる探索分岐が増え、木の幅が不必要に拡大する。
- 自己対戦で収集した学習データにおいて、同じ局面の正解手が重複表現され、方策ラベルが矛盾する。

以下では、これらの問題を解消するための方策をまとめる。

## MCTS 側での対応

### 1. アクション表現を「順序なしの二点集合」に正規化する
- 各手番で生成する合法手を、空点集合からの組み合わせ \(\{A, B\}\)（\(A \leq B\) を満たすように並び替えたもの）として扱う。
- ノード展開時、ソート済みのキーで子ノードをハッシュ化することで、\(A\rightarrow B\) と \(B\rightarrow A\) が同じ子ノードを指す。
- 既存の盤面更新処理には、正規化済みの二点集合を渡し、まとめて盤面に反映する。これにより枝分かれが組み合わせ数に限定され、探索の重複を避けられる。

### 2. 中間ノードを導入して「同一プレイヤーの二連続手」を段階的に展開する
- 1手目を打った段階の仮想ノードを導入し、同一プレイヤーの二手目まで探索を続けてから評価を行う。
- ノードのキーとして盤面と「一手目の座標」を組にすれば、同一手番内の順序違いでも同じ終端ノードに合流する。
- この方法では、ポリシーネットワークの出力を「一手目候補」と「二手目候補」に分けて扱えるため、通常の単手ゲームに近い MCTS 設計で実装できる。

### 3. 盤面の正規化とトランスポジションテーブルの併用
- 盤面そのものをボード対称性や石の順序で正規化したうえでトランスポジションテーブルに保存する。
- すでに探索済みの状態に再訪した場合は同じノードを再利用できるため、順序の違いによる重複探索をさらに抑制できる。

## 学習（方策ネットワーク）側での対応

### 1. アクション空間の再設計
- ネットワークの出力次元を「空点の組み合わせ」に対応づける。例として、空点インデックスを昇順に並べた二項組み合わせにハッシュを割り当てる。
- 自己対戦データから得られた二点集合を同じハッシュに写像することで、順序の違いによるラベル矛盾を解消できる。

### 2. 二段階方策の分解
- 方策ヘッドを二つ用意し、同一プレイヤーの一手目と二手目を逐次サンプリングする設計にする。
- 学習時には、実際に選ばれた二手を順序付きデータとして扱い、推論時は一手目をサンプリング後、残りの合法手から二手目を選ぶ。探索時には MCTS の 2 層展開と連動させ、順序に依存しない評価に統合する。

### 3. データ拡張（順序の平均化）
- 既存データを利用する場合でも、\(A\rightarrow B\) と \(B\rightarrow A\) の両方をデータセットに含めつつ、ラベルは同一ベクトルに平均化（もしくは同一の組み合わせ ID に集約）する。
- これにより、同じ局面に対して複数の正解が存在することをネットワークが矛盾なく学習できる。

## 実装上の補足

- 初手のみ一石である点に注意し、初手専用のアクション表現（単点）と二手目以降の表現（正規化した二点集合）を区別する必要がある。
- 探索効率を高めるためには、空点が多い序盤での組み合わせ爆発を抑える工夫（候補点の事前フィルタリング、ヒューリスティックなプライオリティ付与）が有効である。
- トランスポジションテーブルや Zobrist Hash を導入する場合、ハッシュ更新の際にも「順序なし集合」を前提とした一貫性を保つことが重要である。

## まとめ
- 二手連続打ちによる順序重複は、アクションを順序なし集合として扱う設計、または MCTS を二層構造に変更することで解消できる。
- 方策ネットワークでは、アクション空間やラベルの正規化によって、同一局面に複数の同値な正解が存在する問題を取り除く。
- 上記の対策を組み合わせることで、探索効率と学習安定性の両面から Connect6 に適したアルゴリズム設計が可能になる。

# 学習改善点

## 概要
- 本プロジェクトの自己対戦～学習パイプラインを精査し、データ生成、バッチサンプリング、学習ループ、評価それぞれの改善余地を整理した。
- 改善案は、既存実装が抱える具体的な懸念（例: データの古さ、温度スケジュールの収束速度、損失のスケーリング）に紐づけて記載している。

## 自己対戦データ生成
### エピソード蓄積方法
- `self_play_and_train` では自己対戦で得られたエピソードを `episodes` に追加し続けるが、学習後も破棄されず、常に全履歴を再学習している。【F:app/training_loop.py†L26-L102】
  - 古い局面が過剰に残り、新しい戦略の学習が遅れる懸念があるため、直近の一定数のみ保持するリプレイバッファ方式や、時間減衰したサンプリング重み付けの導入を検討したい。
  - 記憶域の観点からもエピソード数に上限を設け、古いデータを破棄するローテーションを設ける。

### 探索温度と行動選択
- 手番ごとに温度を `temperature *= 0.8` で指数的に下げており、数手でほぼ決定的なポリシーになる。【F:app/training_loop.py†L59-L69】
  - 序盤の多様性が不足しやすく、分布学習が難しくなるため、AlphaZero 本家同様に「序盤は温度=1」「一定手数以降に 0 へ漸近」などターン数依存のスケジュールへ変更する。
  - 探索回数 `num_simulations_train=50` も固定で少なく、対局の進行度に応じた可変設定（終盤だけ回数削減、序盤増加）を検討する。【F:config/models.py†L35-L53】

### MCTS 探索の改善余地
- ルートノードへのディリクレノイズ付与は `current_depth == 0` のみで期待通りだが、合法手マスク後の再正規化など安全策がない点に注意。【F:mcts.py†L42-L99】
  - ネットワークがゼロ確率を出す手があり得るため、合法手で再正規化する処理や、ノイズ強度をゲーム進行度で調整する仕組みを追加したい。
- 探索木はゲーム中通しで再利用しているが、ルート移動時に訪問統計を再正規化しないため、深く探索した分岐の温度計算が偏る可能性がある。ルート遷移時に新しい `Tree` へ差し替えるか、訪問数を正規化する仕組みがあると安定する。

### 終局処理と報酬
- 終局報酬は `len(record)` の偶奇で符号を反転させて保存しており、ターン数の計測ミスや引き分け処理で誤差が出る恐れがある。【F:app/training_loop.py†L69-L72】
  - 盤面の手番情報を保持している `State` を活用し、局面毎の視点に合わせた価値符号を保存しておくことで、後段の符号反転を不要にしてバグを減らせる。
- 投了（resign）判定がなく、明らかな敗勢局面でも最後まで打ち切るため、価値ネットの教師信号がノイズを含みやすい。価値出力に閾値を設けて早期終了を導入する余地がある。

### データ拡張
- `State.feature()` は現在手番視点の 2 チャネルのみであり、回転・反転対称性を利用したデータ拡張が行われていない。【F:game.py†L37-L82】
  - 自己対戦生成時または学習バッチ生成時に 90 度回転や対称写像でエピソードを増やすと、サンプル効率向上が期待できる。

## バッチサンプリングと前処理
- `EpisodeSampler` は各バッチでランダムにエピソードを選び、ターン数だけ盤面を最初から再現して特徴量を作るため、計算コストが高い。【F:training.py†L33-L64】
  - 自己対戦時に盤面特徴量をキャッシュしておき、サンプリングはそのキャッシュを参照する方式へ変えると、学習時間を大幅に削減できる。
- サンプリングは各ターンを一様に選んでいるが、終盤局面ばかり選ばれる偏りが発生しうる。ターン数に応じたリサンプリング重みや、プレイアウト中の訪問数で重み付けする仕組みがあるとデータ多様性が増す。
- 現状はリプレイバッファが単一リストのため、最新バッチが過去バッチより過剰に再利用される。シャッフルとエポック分割を明示して、1 エポックで各サンプルがほぼ一度だけ使われるよう調整したい。

## 学習ループ
- `Trainer.fit` は呼び出し毎にオプティマイザ状態と学習率を完全に初期化するため、モーメンタムや適応的学習率の恩恵が失われ、学習が停滞しやすい。【F:training.py†L109-L165】
  - 最新データに合わせる狙いは理解できるが、ウォームスタートを許可するフラグを追加し、継続学習時は状態を維持するオプションが望ましい。
- 方策損失・価値損失ともにバッチ内で総和を取っており、バッチサイズに応じて勾配ノルムが変動する。【F:training.py†L141-L158】
  - `mean()` で正規化し、ハイパーパラメータに依存しない学習率設計へ改良する。損失比率をハイパーパラメータ化し、価値損失へ重み付けを行う余地もある。
- エポック毎に学習率へ単純減衰を掛けているが、`num_train_steps` のタイミングで毎回同じ回数だけ減衰が掛かるため、長期学習では過度に学習率が低下する可能性がある。【F:app/training_loop.py†L83-L99】【F:training.py†L160-L177】
  - 学習ステップ数に基づいたスケジューラ（Cosine Annealing や OneCycleLR）を導入し、外部から差し替えやすいよう `Trainer` へフックを追加すると柔軟性が高まる。

## 評価とモデル更新
- 評価はランダムプレイヤーとの対戦のみで、自己対戦の進歩を測りづらい。【F:training.py†L186-L205】
  - 過去のベストモデルとのエローレーティング比較や、スキル差の分かる指標を導入する。モデル保存・ロードと連携し、勝率が改善したときだけ重みを更新するフローが望ましい。
- `self_play_and_train` のループ内で学習後も `episodes` をクリアしないため、改善が評価に反映されるまで時間が掛かる。【F:app/training_loop.py†L40-L102】
  - 学習完了後に使用済みエピソードを削除し、評価→自己対戦→学習のサイクルを短く保つ。チェックポイント保存と tensorboard 等の可視化も導入すると、改善の効果検証が容易になる。

## 追加で検討したい運用面の改善
- 学習ログは `logging` のみで保存されており、損失曲線や勝率推移をグラフ化する仕組みがない。【F:training.py†L130-L184】
  - CSV/TensorBoard 出力を追加し、温度・探索回数・学習率の変更が結果へ与える影響を定量的に把握する。
- モデルの保存タイミングが未実装で、長期学習や途中停止に弱い。学習ループ内で定期的に重みを保存し、ベストモデルと最新モデルを分けて管理する。

# アーキテクチャ改善案結果

## 調査対象の整理
- ユーザー要望は `net.py` のモダン化だが、リポジトリでは `network.py` に `Net` クラスが実装されているため、本調査では同ファイルを対象とした。
- 現行モデルは 2 チャネル入力に対して 3×3 畳み込みと残差ブロックを繰り返し、ポリシー・バリューの 2 ヘッドを持つ AlphaZero 風の小型ネットワークである。

## 現在のベースライン構成
- 初段は `Conv` ラッパーで 3×3 の畳み込みと任意のバッチ正規化を提供する。
- `ResidualBlock` は 3×3 畳み込みを 1 層だけ重ねた単純な残差構造で、チャネル数やブロック数は設定ファイルで決定される。
- ポリシーヘッドは 1×1 畳み込みを 2 層重ねた後にソフトマックスで合法手分布を得ている。
- バリューヘッドは 1×1 畳み込みと全結合を経て `tanh` で [-1, 1] に正規化した状態価値を出力する。

## モダンな代替アーキテクチャ案

### 1. KataGo 系 Residual Network
- **概要**: KataGo (2020-) で採用されている改良版 AlphaZero ネットワーク。より深い残差スタック、グローバル平均プーリング、ポリシーヘッドでの追加特徴結合などが特徴。
- **実装ポイント**:
  - 各残差ブロックにチャネル数 128〜256 程度の 3×3 畳み込みを 2 層積み、最後に Squeeze-and-Excitation やグローバル特徴を混入させる。
  - ポリシーヘッドは 1×1 畳み込みと平行して全局面のグローバル特徴を結合し、最終ロジットへ足し込む方式を採用する。
  - バリューヘッドはグローバル平均プーリング + 小型 MLP (例: 256→64→1) を用いることで過学習を抑える。
- **期待効果**:
  - 同じ探索回数でも評価精度が向上しやすく、盤面全体のパターン認識が強化される。
  - 既存の ResidualBlock を拡張するだけなので段階的な移行が可能。
- **懸念点**:
  - フィルタ数増加に伴い VRAM と計算量が大幅に増える。Self-play の同時実行数に制限が掛かる恐れがある。

### 2. MuZero / EfficientZero 系 Latent Dynamics Network
- **概要**: 学習したモデルで環境遷移を内部的に模倣しつつ、方策・価値・報酬を出力するモデルベース強化学習。AlphaZero を一般化した最新手法。
- **実装ポイント**:
  - 既存の `Net` を「表現ネットワーク」として再設計し、別途「ダイナミクスネットワーク」「予測ネットワーク」を実装する。
  - MCTS が内部状態を展開できるよう、潜在ベクトルと報酬ヘッドの学習を追加する必要がある。
  - 訓練ループは自己対戦軌跡から n-step ターゲットを構築し、ハイブリッド損失（ポリシー・価値・報酬）を最適化するよう書き換える。
- **期待効果**:
  - 探索木のロールアウトが短くても潜在ダイナミクスで先読みでき、サンプル効率が改善する。
  - 無駄な評価を減らし、未知ゲームへの適応力を高められる。
- **懸念点**:
  - 実装規模が大きく、既存コードの大部分を改修する必要がある。
  - ハイパーパラメータが多く学習安定化に追加研究が必要。

### 3. Transformer ベース方策・価値ネットワーク
- **概要**: 盤面をパッチ列やマス列として扱い、自己注意機構で長距離依存を学習するアプローチ。AlphaZero 以降の研究でチェス・将棋に適用例が増えている。
- **実装ポイント**:
  - 入力チャネルを線形埋め込みし、位置エンコーディングを付与した上で Transformer エンコーダを複数層積む。
  - ポリシーヘッドは最終トークン表現に線形層を適用、バリューヘッドは [CLS] トークンを MLP に渡す構造が一般的。
  - 計算負荷を抑えるためにローカル注意 (例: Swin Transformer, Axial Attention) を活用する選択肢もある。
- **期待効果**:
  - 盤面全体のグローバルな依存関係を捉えやすく、複雑な戦術パターンの学習が期待できる。
  - 将来的にマルチモーダル特徴（追加メタデータ等）を組み込みやすい。
- **懸念点**:
  - 自己注意は畳み込みよりメモリ使用量が多く、小規模 GPU ではシーケンス長 (盤面サイズ) に制約が掛かる。
  - 既存の Conv ベースと比べてハイパーパラメータ探索が難しい。

## 推奨アプローチ
1. 短期的には KataGo 風 Residual Network を採用し、既存の Conv/ResidualBlock 実装を拡張する。パラメータ増加に合わせてバッチサイズと探索回数のバランス調整が必要。
2. 中期的には Transformer ブロックを一部導入したハイブリッド構成（例: 途中に自己注意ブロックを挿入）を試験し、計算資源に応じたスケーリング戦略を評価する。
3. 長期的な研究テーマとして MuZero 系アーキテクチャへの移行を検討し、学習ループ全体の再設計計画を別途立案する。

## 次のアクション例
- KataGo 形式のヘッド構造を設計し、必要な設定項目（フィルタ数、ブロック数、SE 有無など）を `NetworkConfig` に追加するタスクを起案。
- Transformer 導入のために盤面を一次元配列化するヘルパー関数と位置エンコーディング実装を PoC として作成。
- MuZero 化の影響範囲（`mcts.py`、`training.py`、データ保存形式）を洗い出すための exec_計画.md を別途作成する。
